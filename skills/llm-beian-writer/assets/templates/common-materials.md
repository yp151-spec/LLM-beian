# Common Filing Draft Templates

Replace bracketed placeholders before use. Keep `待补充` markers until facts are verified.

## A. Service Overview

### 服务基本情况

[公司全称] 拟向中华人民共和国境内用户提供 `[服务名称]` 生成式人工智能服务。该服务主要面向 `[目标用户/行业场景]`，提供 `[文本/图片/音频/视频/API/多模态]` 生成能力，用户可通过 `[网站/App/API/小程序/其他入口]` 使用相关功能。

服务的主要流程为：用户完成 `[注册/登录/认证]` 后输入 `[问题/指令/素材]`，系统经过输入安全检测、模型生成、输出安全审核及日志记录后向用户返回结果。对于命中风险策略的输入或输出，系统将采取拒答、改写、人工复核、限制账号或其他处置措施。

### 技术路线

本服务采用 `[自研模型/第三方模型/开源模型/混合架构]`。模型名称及版本为 `[模型名称和版本]`，部署方式为 `[本地私有化/云服务/API调用/混合部署]`。系统由输入处理、内容安全策略、模型推理、检索增强 `[如适用]`、输出审核、日志审计和投诉反馈模块组成。

待补充：模型供应商、部署区域、版本更新机制、回滚机制、上线前评估记录。

## B. Corpus And Data Source Statement

### 语料来源与合法性

服务涉及的训练、优化训练或知识库数据主要包括 `[数据类别列表]`。相关数据来源包括 `[公开数据/自有业务数据/授权数据/供应商数据/用户上传内容/其他]`。公司已通过 `[合同/授权书/公开许可/内部审批/数据清单]` 对数据来源、使用范围、知识产权和个人信息处理基础进行审核。

对于可能包含个人信息的数据，公司采取 `[去标识化/脱敏/最小化/权限控制/删除机制]` 等措施，并按照内部数据安全管理要求进行访问控制和留痕。

待补充：数据源清单、授权证明、个人信息处理依据、版权审查记录、境外数据情况。

### 数据质量与过滤

公司在数据入库和使用前执行质量控制，包括格式校验、重复内容清理、低质量内容过滤、敏感信息识别、违法不良信息过滤、版权风险排查和人工抽样复核。对发现的问题数据建立移除、纠错和追溯机制。

待补充：过滤规则、抽样比例、质量指标、问题数据处置记录。

## C. Labeling Rules Summary

### 标注规则与人员管理

如服务涉及人工标注，公司制定标注规则，对标注任务、标签定义、正反例、争议处理、复核流程和质量指标进行明确。标注人员需经过培训后上岗，标注结果经过抽样复核；对争议样本和低质量批次进行返工或剔除。

待补充：标注手册、培训记录、复核比例、合格率、争议样本处理记录。

## D. Security Assessment Report Outline

### 1. 评估对象

- 服务名称：`[服务名称]`
- 备案主体：`[公司全称]`
- 服务入口：`[URL/App/API]`
- 模型及版本：`[模型名称/版本]`
- 评估范围：`[功能范围、用户范围、数据范围]`

### 2. 风险识别

围绕违法不良信息、歧视偏见、个人信息泄露、知识产权风险、虚假信息、未成年人保护、网络安全滥用、行业场景风险等方面识别风险。

### 3. 安全措施

- 输入侧：敏感词和语义识别、账号风控、频率限制、恶意提示识别。
- 模型侧：安全对齐、拒答策略、领域边界控制、版本评估。
- 输出侧：安全分类器、关键词/语义审核、人工复核、风险内容处置。
- 运营侧：投诉举报、日志审计、应急响应、整改闭环。

### 4. 测试与结果

测试集覆盖 `[风险类别]`，采用 `[自动化测试/人工评测/红队测试/抽样复核]`。记录通过率、拦截率、误拦截情况、典型失败样本和整改措施。

待补充：测试集规模、抽样方法、评估指标、失败样本、整改前后对比。

### 5. 结论与整改计划

经评估，服务已建立 `[已落实措施]`。对于 `[遗留风险]`，计划通过 `[整改措施]` 在 `[时间]` 前完成优化，并持续记录评估和整改结果。

## E. User Agreement And Privacy Points

### 用户协议要点

用户应遵守法律法规和平台规则，不得利用服务生成、传播违法不良信息，不得侵害他人个人信息、肖像权、名誉权、知识产权或商业秘密。平台可对违规行为采取提示、拒绝生成、限制功能、暂停或终止服务等措施。

### 隐私政策要点

说明收集的信息类型、处理目的、保存期限、共享对象、用户权利路径、投诉联系方式、未成年人保护规则以及账号注销/数据删除方式。

## F. Regulator Supplement Reply Template

针对贵单位提出的 `[问题编号/补充要求]`，我司补充说明如下：

1. 事实情况：`[客观描述]`
2. 已采取措施：`[制度、技术、人员、流程、证据]`
3. 附件材料：`[附件名称]`
4. 后续计划：`[整改或持续优化安排]`

以上内容均基于当前服务版本 `[版本号]` 和截至 `[日期]` 的内部记录。若服务功能、模型版本或数据来源发生重大变化，我司将按要求更新相关材料。
